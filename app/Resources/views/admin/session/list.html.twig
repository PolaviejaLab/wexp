		<p><h2>Sessions</h2></p>

		<div class="help-block">
			<p>An overview of the current and past runs of the experiment.</p>
		</div> 		
		
		<table class="table table-hover">
			<thead>
				<tr>
					<th>Date</th>
					<th>Status</th>
				</tr>
			</thead>
	
			<tbody>
				{% for session in sessions %}
				<tr>
					<td>{{ session.started|date('Y-m-d h:i') }}</td>
					<td>
						<a href="{{ path('admin_session_details', {'session': session.id}) }}">
						{% if session.status == 0 %}
							Waiting
						{% endif %}
						
						{% if session.status == 1 %} 
						Running
						{% endif %}
						
						{% if session.status == 2 %}
						Finished
						{% endif %}
						</a>
					</td>
				</tr>
				{% endfor %}				
			</tbody>
		</table>

		<p>
			<a class="btn btn-primary">
				<i class="glyphicon glyphicon-download"></i>
				Download dataset
			</a>
		</p>