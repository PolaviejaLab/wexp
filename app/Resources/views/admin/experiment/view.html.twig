{% extends 'base.html.twig' %}

{% block body %}

<div class="page-header">
	<h1>{{ experiment.name }}</h1>
</div>

<div class="row">

	<div class="col-md-4">{% render(controller('AppBundle:ScreenAdmin:listByExperiment', {'experiment': experiment.id})) %}</div>
	
	<!--
	<div class="col-md-4">{% render(controller('AppBundle:RoleAdmin:listByExperiment', {'experiment': experiment.id})) %}</div>
	-->

	<!-- <div class="col-md-4">
		<p><h2>Participation conditions</h2></p>

		<p>Everyone can participate.</p>

		<p>
			<a class="btn btn-primary">Add new rule</a>
		</p>
	</div>  
	-->

	<!--
	<div class="col-md-4">
		<p><h2>Global scripts</h2></p>

		<div class="help-block">
			<p>Global scripts augment the experiment logic associated with
			individual screens, allowing separation of shared logic.</p>
		</div> 		
		
		<table class="table table-hover"> 
			<thead>
				<tr>
					<th>Name</th>
					<th>Type</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td><a href="">Default script</a></td>
					<td>JavaScript</td>
					<td></td>
				</tr>
			</tbody>
		</table>

		<p>			
			<a class="btn btn-primary">
				<i class="glyphicon glyphicon-plus"></i>
				Script
			</a>
		</p>
	</div>
	-->

	<div class="col-md-4">{% render(controller('AppBundle:SessionAdmin:listByExperiment', {'experiment': experiment.id})) %}</div>
</div>

{% endblock %}